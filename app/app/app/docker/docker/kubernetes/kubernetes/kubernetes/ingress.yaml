apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: advanced-ingress
spec:
  rules:
  - host: app.local
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: advanced-service
            port:
              number: 80
